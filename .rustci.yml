jobs:
  - name: install
    run: npm ci
    image: node:20

  - name: lint
    run: npm run lint
    image: node:20

  - name: test
    run: npm test -- --watchAll=false
    image: node:20

  - name: build
    run: npm run build
    image: node:20
    artifacts:
      - build/**
      - coverage/**